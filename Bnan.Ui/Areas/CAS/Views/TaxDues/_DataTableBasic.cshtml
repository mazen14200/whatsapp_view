@using Bnan.Core.Extensions
@using Bnan.Ui.ViewModels.CAS

@model List<CrCasAccountContractTaxOwed>
@inject IViewLocalizer localizer
@inject IOptions<RequestLocalizationOptions> options
@{
    var requestCulture = Context.Features.Get<IRequestCultureFeature>();
    ViewData["returnUrl"] = string.IsNullOrEmpty(Context.Request.Path) ? "~/" : $"~{Context.Request.Path.Value}";
}
@* 
  "EmployeeInformationCode": "Code",
  "CarContractId": "ID",
  "EmployeeInformationNationality": "Nationality",
 
 *@
 <style>
    #_Nc{
        text-align: center;
    }
 </style>
<style>
    ._Tr_black {
        border-image:fill;
    }

    ._Tr_red {
        color:red;
    }
</style>

@{
    var class_color_black = "_Tr_black";
    var class_color_red = "_Tr_red";
    var color_tr = "";

}

@if (requestCulture?.RequestCulture.UICulture.Name == "en-US")
{
    
       <div class="col-sm-12">
        @* For-scrolling *@
        <table id="basic-datatable" class="table table-bordered dt-responsive nowrap dataTable no-footer dtr-inline table-hover" role="grid" aria-describedby="basic-datatable_info" style="position: relative; width: 1166px; ">

            <thead>
                <tr role="row">
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["No"]</th>
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["TaxOwed_ContractNo"]</th>
                    <th style="text-align:start" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_duedate"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_ContractValue"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_Percentage"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_Value"]</th>
                    <th data-orderable="false">@localizer["TaxOwed_Status"]</th>
                    <th style="text-align:start" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_Pay_Date"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_Pay_No"]</th>
                </tr>
            </thead>
            <tbody>

                @{
                    int no = 1;
                }
                @foreach (var item in Model)
                {

                        color_tr = class_color_black;
                    @*<tr onclick="location.href='@Url.Action("Edit", "CarContract", new { id = item.CrCasEmployeeContractBasic4.CrCasEmployeePrivateCarInformationId })'" style="cursor:pointer;color:red">*@
                    <tr class="@color_tr" >
                        <td id="_Nc">@no</td>
                        <td id="_Nc">@item.CrCasAccountContractTaxOwedContractNo</td>
                        @{
                            <td style="text-align:start">@item.CrCasAccountContractTaxOwedDate?.ToString("dd/MM/yyyy",CultureInfo.InvariantCulture)</td>
                            <td id="_Nc">@item.CrCasAccountContractTaxOwedContractValue?.ToString("N2",CultureInfo.InvariantCulture)</td>
                            <td id="_Nc">@item.CrCasAccountContractTaxOwedPercentage?.ToString("N2",CultureInfo.InvariantCulture)</td>
                            <td id="_Nc">@item.CrCasAccountContractTaxOwedValue?.ToString("N2",CultureInfo.InvariantCulture)</td>
                            <td>
                                @if (item.CrCasAccountContractTaxOwedIsPaid == true)
                                {
                                    @localizer["status_Is_Paid"]
                                }
                                else
                                {
                                    @localizer["status_Not_Paid"]
                                }
                                
                            </td>
                            <td style="text-align:start">@item.CrCasAccountContractTaxOwedPayDate?.ToString("dd/MM/yyyy",CultureInfo.InvariantCulture)</td>
                            <td id="_Nc">@item.CrCasAccountContractTaxOwedPayCode</td>
                        }
                        </tr>
                    
                    
                    no++;
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="col-sm-12">
        @* For-scrolling *@
        <table id="basic-datatable" class="table table-bordered dt-responsive nowrap dataTable no-footer dtr-inline table-hover" role="grid" aria-describedby="basic-datatable_info" style="position: relative; width: 1166px; ">
            <thead>
                <tr role="row">
                    <th  id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["No"]</th>
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["TaxOwed_ContractNo"]</th>
                    <th style="text-align:start" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_duedate"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_ContractValue"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_Percentage"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_Value"]</th>
                    <th data-orderable="false">@localizer["TaxOwed_Status"]</th>
                    <th style="text-align:start" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_Pay_Date"]</th>
                    <th id="_Nc" aria-label="Position: activate to sort column ascending">@localizer["TaxOwed_Pay_No"]</th>
                </tr>
            </thead>
            <tbody>

                @{
                    int no = 1;
                }
                @foreach (var item in Model)
                {
                        color_tr = class_color_black;

                    @*<tr onclick="location.href='@Url.Action("Edit", "CarContract", new { id = item.CrCasEmployeeContractBasic4.CrCasEmployeePrivateCarInformationId })'" style="cursor:pointer;color:red">*@
                    <tr class="@color_tr" >
                        <td id="_Nc">@no</td>
                        <td id="_Nc">@item.CrCasAccountContractTaxOwedContractNo</td>
                        @{
                            <td style="text-align:start">@item.CrCasAccountContractTaxOwedDate?.ToString("yyyy/MM/dd",CultureInfo.InvariantCulture)</td>
                            <td id="_Nc">@item.CrCasAccountContractTaxOwedContractValue?.ToString("N2",CultureInfo.InvariantCulture)</td>
                            <td id="_Nc">@item.CrCasAccountContractTaxOwedPercentage?.ToString("N2",CultureInfo.InvariantCulture)</td>
                            <td id="_Nc">@item.CrCasAccountContractTaxOwedValue?.ToString("N2",CultureInfo.InvariantCulture)</td>
                            <td>
                                @if (item.CrCasAccountContractTaxOwedIsPaid == true)
                                {
                                    @localizer["status_Is_Paid"]
                                }
                                else
                                {
                                    @localizer["status_Not_Paid"]
                                }

                            </td>
                            <td style="text-align:start">@item.CrCasAccountContractTaxOwedPayDate?.ToString("yyyy/MM/dd",CultureInfo.InvariantCulture)</td>
                            <td id="_Nc">@item.CrCasAccountContractTaxOwedPayCode</td>
                        }
                    </tr>


                    no++;
                }
            </tbody>
        </table>
    </div>
}