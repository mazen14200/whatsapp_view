

@using Bnan.Core.Extensions
@model IEnumerable<CrCasRenterContractBasic>
@inject IViewLocalizer localizer
@inject IOptions<RequestLocalizationOptions> options
@{
    var requestCulture = Context.Features.Get<IRequestCultureFeature>();
    ViewData["returnUrl"] = string.IsNullOrEmpty(Context.Request.Path) ? "~/" : $"~{Context.Request.Path.Value}";
}
@* 
  "RenterInformationCode": "Code",
  "DriverContractId": "ID",
  "RenterInformationNationality": "Nationality",
  "RenterInformationAddress": "Address",
  "RenterInformationProfession": "Profession",
  "RenterInformationWorkPlace": "Work Place",
 
  
 *@
@if (requestCulture?.RequestCulture.UICulture.Name == "en-US")
{
    
       <div class="col-sm-12">
        @* For-scrolling *@
        <table id="basic-datatable" class="table table-bordered dt-responsive nowrap w-100 dataTable no-footer dtr-inline table-hover" role="grid" aria-describedby="basic-datatable_info" style="position: relative; width: 1166px; ">

            <thead>
                <tr role="row">
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["No"]</th>
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["DriverContractId"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["RenterInformationName"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["DateLast_Contract"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Contract_Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Days_Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["KM_Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Status"]</th>
                </tr>
            </thead>
            <tbody>

                @{
                    int no = 1;
                }
                @foreach (var item in Model)
                {

                    @*<tr onclick="location.href='@Url.Action("Edit", "DriverContract", new { id = item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationId })'" style="cursor:pointer;color:red">*@
                    <tr onclick="location.href='@Url.Action("Edit", "DriverContract", new { id = item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationId })'" >
                        <td id="_Nc">@no</td>
                            <td id="_Nc">@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationId</td>
                            <td>@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationEnName</td>
                            <td>@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationLastContract?.ToString("dd/MM/yyyy")</td>

                            <td id="_Nc">@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationContractCount</td>
                            <td id="_Nc">@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationDaysCount</td>
                            <td id="_Nc">@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationTraveledDistance</td>

                            
                            <td>

                                @if (@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationStatus == Status.Active)
                                {
                                    @localizer["Active"]
                                }
                                else if (@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationStatus == Status.Rented)
                                {
                                @localizer["Rented_n"]
                                }
                                else if (@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationStatus == Status.Hold)
                                {
                                    @localizer["Hold"]
                                }
                            else if (@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationStatus == Status.Deleted)
                                {
                                    @localizer["Deleted"]
                                }
                                else
                                {

                                }
                            </td>
                        </tr>
                    
                    
                    no++;
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="col-sm-12">
        @* For-scrolling *@
        <table id="basic-datatable" class="table table-bordered dt-responsive nowrap w-100 dataTable no-footer dtr-inline table-hover" role="grid" aria-describedby="basic-datatable_info" style="position: relative; width: 1166px; ">
            <thead>
                <tr role="row">
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["No"]</th>
                    <th id="_Nc" aria-sort="ascending" aria-label="Name: activate to sort column descending">@localizer["DriverContractId"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["RenterInformationName"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["DateLast_Contract"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Contract_Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Days_Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["KM_Count"]</th>
                    <th aria-label="Position: activate to sort column ascending">@localizer["Status"]</th>
                </tr>
            </thead>
            <tbody>

                @{
                    int no = 1;
                }
                @foreach (var item in Model)
                {

                    @*<tr onclick="location.href='@Url.Action("Edit", "DriverContract", new { id = item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationId })'" style="cursor:pointer;color:red">*@
                    <tr onclick="location.href='@Url.Action("Edit", "DriverContract", new { id = item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationId })'">
                        <td id="_Nc">@no</td>
                        <td id="_Nc">@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationId</td>
                        <td>@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationArName</td>
                        <td>@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationLastContract?.ToString("dd/MM/yyyy")</td>

                        <td id="_Nc">@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationContractCount</td>
                        <td id="_Nc">@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationDaysCount</td>
                        <td id="_Nc">@item.CrCasRenterContractBasic4?.CrCasRenterPrivateDriverInformationTraveledDistance</td>

                        
                        <td>

                            @if (@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationStatus == Status.Active)
                            {
                                @localizer["Active"]
                            }
                            else if (@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationStatus == Status.Rented)
                            {
                                @localizer["Rented_n"]
                            }
                            else if (@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationStatus == Status.Hold)
                            {
                                @localizer["Hold"]
                            }
                            else if (@item.CrCasRenterContractBasic4.CrCasRenterPrivateDriverInformationStatus == Status.Deleted)
                            {
                                @localizer["Deleted"]
                            }
                            else
                            {

                            }
                        </td>
                    </tr>
                    
                    
                    no++;
                }
            </tbody>
        </table>
    </div>
}